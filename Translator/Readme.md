# Translator Engine 

__esto funciona de la siguiente manera __

translator engine se creo, porque en las carpetas 📁 "VuStrings"
habia unos binarios que no se extraían así que en vez de arruinar el
extractor para que lea y lo convierta a JSON eh pensando otra forma 
